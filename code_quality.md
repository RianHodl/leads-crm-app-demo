# Code Quality Review – `server.ts` Changes

**Summary of changes**
- Added a new **search endpoint** `GET /api/leads/search`.
- Implemented input validation (required query, length limit, whitelist characters).
- Added pagination support (`limit` & `offset`) with sensible defaults and a hard‑cap of 100 rows.
- Switched to **parameterised SQL** using placeholders to avoid SQL‑injection.
- Added basic error logging.

## Correctness
| Aspect | Observation | Recommendation |
|--------|-------------|----------------|
| **SQL safety** | Uses prepared statements (`?` placeholders) and validates the search string before building the `LIKE` pattern. | ✅ No change needed. |
| **Input validation** | Checks for presence, length (≤ 100), and allowed characters (`^[a-zA-Z0-9 @.]*$`). | Consider normalising whitespace (e.g., trim) before validation. |
| **Pagination** | `limit` defaults to 20, capped at 100; `offset` defaults to 0. | Verify that `parseInt` on non‑numeric strings yields `NaN` → fallback works; you may want explicit `Number.isNaN` guard for clarity. |
| **Error handling** | Catches DB errors and returns `500` with a generic message, logging the full error to console. | ✅ Acceptable for a demo app. |
| **Response shape** | Returns only selected columns, which is good for reducing payload. | ✅ No change needed. |

## Readability & Style
- The endpoint is well‑structured and clearly separated from other routes.
- Variable names (`rawQuery`, `rawLimit`, `rawOffset`) make it obvious they are raw strings.
- Inline comments explain each validation step.
- The use of back‑ticks for the SQL string keeps it readable.

### Minor style tweaks
1. **Consistent quoting** – you mix double quotes (`"`) for keys and back‑ticks for the template literal. Choose one style (e.g., single‑quotes for strings) for consistency.
2. **Logging** – use `console.error` for error cases (already done) and `console.debug` for successful searches if you need verbose output.
3. **Extract constants** – `MAX_QUERY_LENGTH = 100` and `MAX_LIMIT = 100` could be top‑level constants to avoid magic numbers.

## Potential Issues & Recommendations
1. **SQL wildcard performance** – `%${query}%` prevents index use. If the dataset grows, consider full‑text search (FTS5) in SQLite.
2. **Rate limiting / abuse** – No protection against extremely frequent calls. Add a simple rate‑limiter or debounce on the client side.
3. **International characters** – The whitelist excludes accented characters. If you need i18n support, broaden the regex.
4. **Testing** – Add unit tests for the endpoint covering:
   - Missing query → 400
   - Too long query → 400
   - Invalid characters → 400
   - Normal query returns expected rows
   - Pagination boundaries

## Overall Assessment
The added search functionality is **functionally correct**, **secure**, and **readable**. The main opportunities are minor style consistency, extracting magic numbers to constants, and adding tests / performance considerations for larger datasets.

---
*Generated by Antigravity’s code‑quality review skill.*
